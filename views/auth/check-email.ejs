<%- include('../partials/header') %>

<div class="row justify-content-center">
  <div class="col-md-5 text-center">
    <i class="bi bi-envelope-check display-4 text-primary"></i>
    <h1 class="h4 mt-3">Check your email</h1>
    <p class="text-muted">
      If <strong><%= email %></strong> has an account, a sign-in link is on its way.
      The link expires in 15 minutes and must be opened in this browser.
    </p>
  </div>
</div>

<%- include('../partials/footer') %>

# Secrets / local config — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Runtime logs
logs/*.log

# Reporter persistence (dedup state + audit log)
data/

# OS / editor cruft
.DS_Store
*.swp
